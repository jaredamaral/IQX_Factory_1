# Reference-only folders (provided to inform the build; not part of this repo)
IQX_Factory_Claude/
Sigma_Skills/

# Secrets — never commit Sigma/Snowflake credentials
.env
**/.env
!**/.env.example

# OS / editor noise
.DS_Store
Thumbs.db

# Python / node tooling caches (if used by scripts)
__pycache__/
*.pyc
node_modules/
