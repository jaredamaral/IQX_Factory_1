# Reference-only folders (provided to inform the build; not part of this repo)
IQX_Factory_Claude/
Sigma_Skills/

# Official Sigma skills — external dependency installed via factory/scripts/install-sigma-skills.ps1
.cursor/skills/sigma-api/
.cursor/skills/sigma-data-models/

# Secrets — never commit Sigma/Snowflake credentials
.env
**/.env
!**/.env.example

# OS / editor noise
.DS_Store
Thumbs.db

# Python / node tooling caches (if used by scripts)
__pycache__/
*.pyc
node_modules/
