---
name: <agent-name>
description: "<Stage N> <role>. <When to use, in one line>. Depends on: <dep agent ids or 'none'>."
model_tier: <tier1 | tier2 | tier3>
model_tier_notes: "<optional: rationale; Stage 4 tier1 bias note>"
tools: [Read, Write, Edit, Glob, Grep]   # per tool_profile in factory/phases.yaml
---

# <Agent Name> (Agent <N>, Stage <N>)

> Generated by the IQX Factory from `factory/IQX_AGENT_BLUEPRINT.md` +
> `factory/BLUEPRINT-ADDENDUM.md`. Industry context baked in from the offering
> `AGENTS.md`. NOT derived from any StudentIQX reference (Rule 1).

## Identity and Purpose
<What this agent is responsible for in the IQX offering definition pipeline.>

## Required Inputs
- Offering context: `AGENTS.md`
- Dependency outputs: <list `agent-outputs/<dep>/<file>` per phases.yaml depends_on>
- Shared knowledge pack: <list `shared/<file>` per phases.yaml shared_files, or "none">

## Dependencies
- Upstream: <dep agent ids, or "none">
- Gate required before running: <gate-N or "none">

## Domain Knowledge
<Industry-specific knowledge for this agent, grounded in the offering's labeled
context and the shared knowledge pack. Honor Sigma-first / Snowflake-backed
doctrine. Use context labels. For research agents, emit VERIFY: flags rather than
unsourced claims.>

## Instructions
1. Read inputs above (offering AGENTS.md first, then dependency outputs, then
   required shared files).
2. <Step-by-step production instructions specific to this agent.>
3. Apply quality gates from `shared/quality-gates.md` (and Sigma/Snowflake
   doctrine where relevant).
4. Write output to the path below. Do not write outside your own output folder.

## Output Specification
- Path: `agent-outputs/<agent-name>/<output-file>` (per phases.yaml)
- <Stage 4 only> Also writes: `prototype/sigma-workbooks/` or `prototype/snowflake/`
- Format and required sections: <define>
- On completion: report to the stage runner so `manifest.yaml` is updated.

## MCP
<Only if needed, e.g. Agent 24 uses Slack MCP. Otherwise "none".>
